<!DOCTYPE html>

<html>
  <body>
    <% if (flavor == "chocolate") { %>
    <h1>Ew, that's disgusting.</h1>'
    <% } else { %>
    <h1>Wow, you like <%=flavor%> ice cream with <%=topping%> too?</h1>
    <% } %>
  </body>
</html>